name: Deploy to Namecheap (build + rsync)

on:
  push:
    branches: ["main", "dev", "qa", "sandbox"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ----------------------------
      # Build (my-app)
      # ----------------------------
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: my-app/package-lock.json

      - name: Install deps
        working-directory: my-app
        run: npm ci

      - name: Build
        working-directory: my-app
        run: npm run build

      - name: Ensure build output exists
        run: |
          test -d "my-app/dist" || (echo "Build output not found at my-app/dist (if it's build/, tell me)" && ls -la my-app && exit 1)
          ls -la my-app/dist

      # ----------------------------
      # SSH setup
      # ----------------------------
      - name: Install SSH key from base64
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SFTP_KEY_B64 }}" | base64 -d > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

      # ----------------------------
      # Target folder mapping
      # prod   : flukegamestudio.com
      # dev    : flukegamestudio.com/dev
      # qa     : flukegamestudio.com/qa
      # sandbox: flukegamestudio.com/sandbox
      # ----------------------------
      - name: Choose remote folder
        id: target
        run: |
          case "${{ github.ref_name }}" in
            main)    echo "REMOTE_DIR=public_html" >> $GITHUB_OUTPUT ;;
            dev)     echo "REMOTE_DIR=public_html/dev" >> $GITHUB_OUTPUT ;;
            qa)      echo "REMOTE_DIR=public_html/qa" >> $GITHUB_OUTPUT ;;
            sandbox) echo "REMOTE_DIR=public_html/sandbox" >> $GITHUB_OUTPUT ;;
            *)       echo "REMOTE_DIR=public_html" >> $GITHUB_OUTPUT ;;
          esac

      - name: Ensure remote dir exists
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USER: ${{ secrets.SFTP_USER }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          REMOTE_DIR: ${{ steps.target.outputs.REMOTE_DIR }}
        run: |
          ssh -p "$SFTP_PORT" \
            -i ~/.ssh/deploy_key \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            "$SFTP_USER@$SFTP_HOST" \
            "mkdir -p ~/${REMOTE_DIR} && ls -la ~/${REMOTE_DIR}"

      # ----------------------------
      # Deploy dist (replace/update only, NO deletions)
      # ----------------------------
      - name: Rsync deploy dist (no deletions)
        env:
          SFTP_HOST: ${{ secrets.SFTP_HOST }}
          SFTP_USER: ${{ secrets.SFTP_USER }}
          SFTP_PORT: ${{ secrets.SFTP_PORT }}
          REMOTE_DIR: ${{ steps.target.outputs.REMOTE_DIR }}
        run: |
          rsync -az \
            -e "ssh -p ${SFTP_PORT} -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null" \
            my-app/dist/ \
            "${SFTP_USER}@${SFTP_HOST}:~/${REMOTE_DIR}/"